# Codex Project Instructions

This folder stores project-specific guidance for Codex sessions.

Before editing this project, read:

- `CODEX_CONTEXT.md`

Core rules:

- Talk with the user in Portuguese.
- Keep the application UI in English.
- Preserve the working single-phase flow.
- Evolve three-phase support inside the same application version.
- Avoid changing persisted JSON/internal names unless compatibility is handled.
- Use `apply_patch` for manual edits.
- Run the validation snippets from `CODEX_CONTEXT.md` after code changes when practical.

